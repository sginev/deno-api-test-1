type Query {
  auth: String!
  persons: [Person]
  person(username:String): Person
}

interface Node {
  id: ID!
}

type Person {
  id: String!
  lastname: String
  firstname: String
  address: String
  motto: String
  avatar: String
  username: String
  password: String
  job: String
  phone: String
}